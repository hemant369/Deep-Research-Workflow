# Next.js Frontend

Run from `frontend/`:

```powershell
npm.cmd install
npm.cmd run dev
```

The frontend expects the FastAPI backend at `http://localhost:8000`.
Override with `NEXT_PUBLIC_API_BASE_URL` in `.env.local`.
